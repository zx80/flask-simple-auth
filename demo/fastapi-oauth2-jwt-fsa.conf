import os

FSA_MODE = "dev"
FSA_REALM = "demo"
FSA_AUTH = "token"
FSA_PARAM_USER = "username"
FSA_PARAM_PASS = "password"
FSA_TOKEN_TYPE = "jwt"
FSA_TOKEN_DELAY = 30.0  # expires after 30 minutes
FSA_TOKEN_SECRET = os.environ["DEMO_SECRET"]
