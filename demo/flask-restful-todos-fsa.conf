# basic authentication, password "sesame" in "plaintext"
FSA_AUTH = ["basic"]  # only basic, disable "token"
FSA_PASSWORD_SCHEME = "plaintext"
FSA_GET_USER_PASS = lambda _: "sesame"
# FSA_MODE = "debug"
# NOTE with caching 500 errors can occur after a delete
# FSA_CACHE = None
